{
  "name": "design-audit-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Design-system integrity auditor: detect contrast failures and cross-source drift (code, Style Dictionary, Figma, Storybook).",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@design-audit/core --workspace=@design-audit/react",
    "test": "node --test packages/core/test"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
